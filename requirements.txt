# LLMCloak runtime dependencies
fastapi>=0.110
uvicorn>=0.29
cryptography>=41.0
# optional: high-performance egress index (falls back to pure-python scan if missing)
pyahocorasick>=2.0

# test / E2E
httpx>=0.27
pytest>=8.0
